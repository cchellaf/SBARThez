# torch: install the build matching your CUDA version from
# https://pytorch.org/get-started/locally/ instead of a bare `pip install torch`
# if you need GPU support -- the plain PyPI wheel may give you a CPU-only build.
torch>=2.1

transformers>=4.40
sentencepiece>=0.1.99
protobuf>=3.20
jsonlines>=4.0
FlagEmbedding>=1.2          
kaldi_io>=0.9.4             
numpy>=1.24
PyYAML>=6.0                
evaluate>=0.4               
rouge_score>=0.1.2          
bert_score>=0.3.13